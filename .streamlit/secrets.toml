# ====================== ERM Dashboard Secrets ======================
# Used by app.py (we'll update the dashboard in Fix #3)

# Backend URL (change this if you rename your Render service)
BACKEND_URL = "https://erm-live.onrender.com"

# Optional: Add any future secrets here (Open-Meteo key is not needed — we use the free public API)
