# =============================================
# ERM v8.0 — Unified requirements.txt
# Covers BOTH FastAPI backend AND Streamlit dashboard
# Install with: pip install -r requirements.txt
# =============================================

# === FastAPI Backend (core) ===
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
pandas>=2.2.0
numpy>=1.26.0
aiofiles>=24.1.0
pydantic>=2.8.0
python-multipart>=0.0.9

# === Streamlit Dashboard ===
streamlit>=1.38.0
plotly>=5.23.0
requests>=2.32.0

# === Shared utilities (already covered above but explicit for clarity) ===
# pandas, numpy already listed

# =============================================
# Optional: Development / CI tools (uncomment if needed)
# =============================================
# ruff>=0.6.0
# black>=24.8.0
# pytest>=8.3.0
# pytest-asyncio>=0.23.0
# bandit>=1.7.0
# safety>=2.3.0
# pyright>=1.1.0

# =============================================
# Notes:
# • This file is intentionally minimal and version-flexible for Render + GitHub Actions.
# • All packages used in main.py (FastAPI) and app.py (Streamlit) are included.
# • No conflicting versions — tested together with Python 3.12.
# • Render.yaml and GitHub Actions both reference this file implicitly.
# =============================================
